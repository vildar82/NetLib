<wpf:BaseWindow x:Class="WpfApp1.MainView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:wpfApp1="clr-namespace:WpfApp1"
                                   xmlns:wpf="clr-namespace:NetLib.WPF;assembly=NetLib"
                                   mc:Ignorable="d" 
                d:DataContext="{d:DesignInstance wpfApp1:MainViewModel, d:IsDesignTimeCreatable=True}" Title="MainView"
        Name="window">
    <Grid>
		<StackPanel>
			<TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
			<TextBlock Text="{Binding Text}"/>
			<TextBlock Text="{Binding Text2}"/>
			<Button Content="Test" Command="{Binding Test}" Margin="10" Width="80" Height="30" />
		</StackPanel>
	</Grid>
</wpf:BaseWindow>